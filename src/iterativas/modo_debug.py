modo_debug = False

for i in range(100):
    if not modo_debug:
        pass
    else:
        print(f"Procesando iteración {i}")

# Controla la salida del programa usando una variable que activa o desactiva el modo debug.