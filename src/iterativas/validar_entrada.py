entrada = ""

while not entrada.isdigit():
    entrada = input("Introduce un número: ")

print("Número válido:", entrada)

# Solicita al usuario un número válido y repite la entrada hasta que cumpla la condición.